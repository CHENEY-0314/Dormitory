<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dormitory">

    <!-- 获取联网权限 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 获取读取SD卡\相机的权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"

        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-library android:name="org.apache.http.legacy" android:required="false" />
        <activity android:name=".Administrator.ManageApply.admManageApply"/>
        <activity android:name=".Student.MyPagesActivity.MyChangeDormitory.ChangeChangeApplyActivity" />
        <activity android:name=".Student.MyPagesActivity.MyFixDormitory.ChangeFixApplyActivity" />
        <activity android:name=".Administrator.ReleaseNote.ReleaseNoteActivity" />
        <activity android:name=".Administrator.AdmActivity" />
        <activity android:name=".Student.MainPageActivity.selectResult" />
        <activity android:name=".Login.AdmLogActivity" android:theme="@style/splash_style"/>
        <activity android:name=".Student.MyPagesActivity.MyFixDormitory.MyFixDormitoryActivity" />
        <activity android:name=".Student.MyPagesActivity.MyChangeDormitory.MyChangeDormitoryActivity" />
        <activity android:name=".Login.StuLogActivity" android:theme="@style/splash_style"/>
        <activity android:name=".Student.MyPagesActivity.ChangeUserImgActivity" />
        <activity
            android:name=".Student.MainPageActivity.repairDormitory"
            android:windowSoftInputMode="stateVisible|adjustPan" />
        <activity android:name=".Student.MainPageActivity.changeDormitory" />
        <activity android:name=".Student.MyPagesActivity.AdviceActivity" />
        <activity android:name=".Student.MyPagesActivity.SetupActivity" />
        <activity android:name=".Student.Tabbar" />
        <activity android:name=".Login.LoginActivity"
            android:theme="@style/start_style">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.dormitory.fileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>